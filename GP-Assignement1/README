

**GPS Matcher** is a Python module that helps developers match each GPS coordinate from one list to the closest location in another list using the Haversine formula. It's ideal for geospatial applications like delivery matching, friend-finder apps, or clustering nearby points.

---


- Calculates accurate great-circle distances using the Haversine formula
- Matches each point in a `source` array to the nearest point in a `target` array
- Easy-to-use function with clear input and output formats
- Built for developers — clean and modular Python code

---


```bash
pip install -r requirements.txt
